* Proper documentation for the autotopology.
* Render widges in a similar tiling manner as the GPU.
(bonds and angles are only shown in the center tile which makes making bonds over the PBC weird).
* Auto remove particles within grid distance from the placed particles (update bonds etc) using cell list
* Do not render widgets during running the simulation (hide and show when the simulation is stopped again).
* escape should bring you back to global and cancel all active selections etc, it should bring you back to a clean state.
* clicking on the tabs should take you there. Clicking on place only shows you the information for placement
as you need to select the molecule to place. The same holds for global.
* Lets remove the rescaling on the grid number to make it sit at the minimum, this is not true anyway
for the actual potential (after shifting) can differ substantially (Mi vs WF also different again).
* Swapping to gravity is not really possible during a run which is maybe ok, but maybe we can create a general 
behavior for gravity turned on which removes molecules which cross the Y-boundary?  




